#include "main.h"

/**
 * _abs - computes the absolute value of integer
 * @i: i is  integer
 *
 * Return: integer
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
